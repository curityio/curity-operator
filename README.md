# Curity Operator

Kubernetes operator for managing Curity Identity Server deployments.

## Quick Start

```bash
make build       # Build binary
make test        # Unit tests
make lint        # Lint
make test-e2e    # E2E tests (creates Kind cluster, builds image, runs tests)
```
