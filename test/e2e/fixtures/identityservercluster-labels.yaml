apiVersion: curity.io/v1alpha1
kind: IdentityServerCluster
metadata:
  name: {{ .name }}
  namespace: {{ .namespace }}
spec:
  version: "11.0"
  podLabels:
    team: platform
  podAnnotations:
    prometheus.io/scrape: "true"
