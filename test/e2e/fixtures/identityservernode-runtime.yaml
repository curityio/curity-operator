apiVersion: curity.io/v1alpha1
kind: IdentityServerNode
metadata:
  name: {{ .name }}
  namespace: {{ .namespace }}
spec:
  type: runtime
  role: runtime-role
  identityServerClusterRef:
    name: {{ .clusterName }}
  replicas: 1
  service:
    type: ClusterIP
    port: 8443
